on: [push]

jobs:
  hello_world_job:
    runs-on: self-hosted
    name: A job to say hello
    steps:
          
    - name: Iniciar Proyecto
      run: | 
         npm ci
         npm run build --if-present
        
